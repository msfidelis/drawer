'use strict';

const aws = require('aws-sdk');
const pdf   = require('html-pdf');
const Promise = require('bluebird');

class S3 {

    constructor() {

    };
    
    save(html, key, options = {}) {

    };

    read(key) {

    };
}

module.exports = new S3();